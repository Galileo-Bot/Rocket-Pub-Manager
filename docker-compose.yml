services:
    bot:
        build:
            context: .
            dockerfile: Dockerfile
        restart: unless-stopped
        mem_limit: 320m
        env_file:
            - .env
        environment:
            AYFRI_ROCKETMANAGER_DB_PATH: /app/data/rocketmanager.db
        volumes:
            - ./logs:/app/logs
            - bot_data:/app/data
            - cds_cache:/app/cds

volumes:
    bot_data:
    cds_cache:
